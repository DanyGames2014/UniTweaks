* Prevent a NRE when encountering an invalid recipe
* Make the torch bottom face rendering toggleable
* Automatically disable a mixin incompatible with Nitch
* Add Beta 1.8 Leaves rendering tweak (disabled by default)